# Required PR metadata

```text
Maquina-Intent: .maquina/intents/TEAM-123-short-slug.json
Maquina-Work-Type: backend
Maquina-Validation-Profile: application
```

The intent path must exist in the trusted base revision when the advisory runs. The work type and validation profile must match values declared by the Factory Contract.
